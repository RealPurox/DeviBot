syntax = "proto3";

option java_multiple_files = true;

option java_package = "net.devibot.grpc.provider";

import "entities.proto";
import "messages.proto";

service ProviderService {

    rpc keepAlive (Empty) returns (DefaultSuccessResponse);

}